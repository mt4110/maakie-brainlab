# secrets / env
.env
.env.*
!.env.example

# virtual env / caches
.venv/
__pycache__/
*.pyc

# local runtime
.run/
*.pid

# generated / bulky (keep out of git)
data
index
logs
models
eval/results/
.local/
s3_review_pack_*/

# editors / tools
.vscode/
.direnv/
.trunk/

# macOS junk
.DS_Store
._*

# backups
*.bak
*.bak.*
Makefile.bak.*

# backup variants
*.bak*

# env files (infra)
*.env
infra/*.env*

# review packs (local artifacts)
s3_review_pack_*.tar.gz
*_review_pack_*.tar.gz
review_bundle_*.txt

# review packs (local artifacts)
s3_review_pack_*.tar.gz
*_review_pack_*.tar.gz
review_bundle_*.txt
